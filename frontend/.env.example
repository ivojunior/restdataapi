# Client ID OAuth criado no Google Cloud Console (tipo "Web application"),
# o mesmo domínio/app do GOOGLE_CLIENT_ID configurado no backend (ver
# .env.example na raiz do projeto).
VITE_GOOGLE_CLIENT_ID=
